{
  "name": "library",
  "version": "1.0.0",
  "description": "a book library ",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "pretty": "prettier --write src/**/*.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.5.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@masagroup/ecore": "file:../ecore"
  },
  "prettier": {
    "printWidth": 100,
    "tabWidth": 4
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "verbose": true,
    "testPathIgnorePatterns": [
      "<rootDir>/dist/"
    ]
  }
}
